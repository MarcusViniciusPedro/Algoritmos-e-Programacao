#include <stdio.h>

int main(){
	
	int cont; {
	
	for (cont = 1; cont <= 200; cont = cont + 1) //Fará a contagem de quantos números são divisíveis por 4
	if (cont % 4 == 0)
	printf ("\ndivisiveis por 4: %d", cont);
	}
	
	return 0;
		
}

